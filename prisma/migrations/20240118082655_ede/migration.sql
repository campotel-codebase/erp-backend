BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[LeaveRequest] ADD [status] VARCHAR(20) NOT NULL CONSTRAINT [LeaveRequest_status_df] DEFAULT 'pending';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
